import React from 'react';
import ReactDOM from 'react-dom/client';
import { Demo } from './Demo-CROSS'; // Import CROSS-EXPANSION demo

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Demo />
  </React.StrictMode>
);
